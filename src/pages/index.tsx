import * as React from "react"

import Toolbar from "../components/Toolbar"

// markup
const IndexPage = () => {
	return (
		<main className="p-4 bg-gray-500">
			<Toolbar />
		</main>
	)
}

export default IndexPage
